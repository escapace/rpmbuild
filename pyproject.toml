[project]
name = "rpmbuild"
version = "0.1.0"
dependencies = []
requires-python = ">=3.9"

[tool.uv]
managed = true
exclude-newer = "10 days"

[tool.uv.workspace]
members = []

[tool.setuptools]
py-modules = []

[tool.ruff]
target-version = "py39"

[dependency-groups]
dev = [
    "ruff>=0.15.12",
    "ty>=0.0.34; python_version >= '3.12'",
]

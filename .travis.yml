language: bash
services: docker
env:
  - TEST_SUITE=docker-gc
  - TEST_SUITE=ferm
  - TEST_SUITE=git
  - TEST_SUITE=mosh

script:
  - gpg --keyserver pgp.mit.edu --recv-key 0x671C330913823C5B
  - ./manage build

notifications:
  email:
    on_success: change
    on_failure: always
